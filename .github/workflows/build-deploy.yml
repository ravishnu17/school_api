name: Build and deploy

on: [push , pull_request]

jobs:
  built:
    environment:
      name: Test_API
    env:
        DB_HOST: localhost
        DB_PORT: 5432
        DB_USERNAME: school
        DB_PASSWORD: school
        DB_NAME: school
        SECRET_KEY: ydew78d8uwed9wejd9wed9weidjwiojduiwedt7wediwkdnowejd9owe
        ALGORITHM: HS256
        TOKEN_EXPIRATION: 60

    services:
      postgresql-x64-14:
        image: postgres
        env:
          POSTGRES_PASSWORD: school
          POSTGRES_DB: school
        ports:
        - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    runs-on: ubuntu-latest
    steps:
      # - name: setup postgres
      #   uses: ikalnytskyi/action-setup-postgres@v3
      #   with:
      #     username: ${{secrets.DB_USERNAME}}
      #     password: ${{secrets.DB_PASSWORD}}
      #     database: ${{secrets.DB_NAME}}
      #     port: ${{secrets.DB_PORT}}
      #   id: postgres
      - name: pull from git
        uses: actions/checkout@v2
      - name: Install python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - name: Update pip
        run: python -m pip install --upgrade pip
      - name: Install dependencies
        run: pip install -r requirements.txt  
      - name: Test with pytest
        run: |
          alembic upgrade head

